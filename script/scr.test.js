import { jest } from '@jest/globals';

beforeEach(() => {
  document.body.innerHTML = `
    <div class="wrapper-3">
      <div class="reel"></div>
    </div>
    <div class="inner final"></div>
  `;
});

describe('DOMContentLoaded Event Listener', () => {
});
